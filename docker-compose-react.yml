version: "3.9"

services:
  XenonSms-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: XenonSms-frontend:latest
    container_name: XenonSms-frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
 

